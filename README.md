
  # Duality

  This is a code bundle for Duality. The original project is available at https://www.figma.com/design/v2GzzV0gK9Gv3DFOgs8pP1/Duality.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  